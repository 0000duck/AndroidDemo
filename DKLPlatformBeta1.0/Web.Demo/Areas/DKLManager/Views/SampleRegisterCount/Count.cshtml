@using HYZK.FrameWork.Common
@using HYZK.FrameWork.Web.Controls
@using HYZK.Account.Contract
@using HYZK.FrameWork.Utility

@model Dictionary<string, int> 
@{
    Layout = "~/Views/Shared/_Layout.EditNoSubmit.cshtml";
}
@section PageSpecificStyleSheetIncludes{
    <style type="text/css">
        .form-refer-1 {
            font-weight: 800;
            font-size: large;
            color: black;
        }

        .form-refer-2 {
            font-weight: 800;
            font-size: 16px;
            margin-top: 10px;
            resize: none;
            height: 20px;
            width: 23%;
            text-align: center;
        }

        .form-refer-3 {
            font-weight: 800;
            font-size: large;
            width: 25%;
            margin-top: 10px;
            margin-left: 10px;
            resize: none;
            text-align: center;
        }
    </style>

}
@section PageSpecificJavascriptIncludes{
    <script type="text/javascript">
        $(document).ready(function () {
            $("#BirthDate").datepicker(
                {
                    changeYear: true,
                    changeMonth: true,
                });
        });
    </script>
}
@section MainContent{
    <div class="portlet box red m10">
        <div class="portlet-title">
            <h4><i class="icon-reorder"></i>化学样品个数统计</h4>
            <div class="control-group">
                @{
                    foreach (var item in Model)
                    {
                        
                        <div class="form-refer-1">
                         
                            <textarea class=" form-refer-2" readonly>@item.Key : @item.Value</textarea>
                            
                          
                        
                        </div>
                    }
                }
           

              
                    @*<div class="control-group">
                            <label class="control-label">CMAC：</label>
                            <div class="controls">
                                @Html.TextBoxFor(m => m.CMAC, new { @class = "m-wrap small" })
                                <span class="help-inline">@Html.ValidationMessageFor(m => m.CMAC)</span>
                            </div>
                        </div>
                        <div class="control-group">
                            <label class="control-label">CSTEL：</label>
                            <div class="controls">
                                @Html.TextBoxFor(m => m.CSTEL, new { @class = "m-wrap small" })
                                <span class="help-inline">@Html.ValidationMessageFor(m => m.CSTEL)</span>
                            </div>
                        </div>
                        <div class="control-group">
                            <label class="control-label">CTWA：</label>
                            <div class="controls">
                                @Html.TextBoxFor(m => m.CTWA, new { @class = "m-wrap small" })
                                <span class="help-inline">@Html.ValidationMessageFor(m => m.CTWA)</span>
                            </div>
                        </div>*@
                </div>
            </div>
        </div>
  
}

